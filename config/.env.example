# mcp-atlassian 환경 변수 설정 예시
# 이 파일을 ~/.mcp-atlassian/.env로 복사하고 실제 값으로 변경하세요

# Confluence 설정
CONFLUENCE_URL=https://popupstudio.atlassian.net/wiki
CONFLUENCE_USERNAME=your.email@popupstudio.com
CONFLUENCE_API_TOKEN=your_confluence_api_token

# Jira 설정
JIRA_URL=https://popupstudio.atlassian.net
JIRA_USERNAME=your.email@popupstudio.com
JIRA_API_TOKEN=your_jira_api_token

# ============================================================
# 선택적 설정
# ============================================================

# Jira 프로젝트 필터 (쉼표로 구분)
# JIRA_PROJECTS_FILTER=PROJ,DEV,INFRA

# Confluence 스페이스 필터 (쉼표로 구분)
# CONFLUENCE_SPACES_FILTER=POPUP,DEV,TECH

# 읽기 전용 모드 (안전한 조회 전용)
# READ_ONLY_MODE=true

# 상세 로깅
# MCP_VERBOSE=true

# 특정 도구만 활성화 (쉼표로 구분)
# ENABLED_TOOLS=jira_search,jira_get_issue,confluence_search,confluence_get_page

# ============================================================
# 프록시 설정 (필요한 경우)
# ============================================================

# HTTP/HTTPS 프록시
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=https://proxy.company.com:8443
# NO_PROXY=localhost,127.0.0.1

# SOCKS 프록시
# SOCKS_PROXY=socks5://proxy.company.com:1080

# Jira 전용 프록시
# JIRA_HTTPS_PROXY=https://jira-proxy.company.com:8443

# Confluence 전용 프록시
# CONFLUENCE_HTTPS_PROXY=https://confluence-proxy.company.com:8443

# ============================================================
# Server/Data Center 설정 (온프레미스 환경)
# ============================================================

# Personal Access Token 사용 시
# CONFLUENCE_URL=https://confluence.your-company.com
# CONFLUENCE_PERSONAL_TOKEN=your_personal_access_token
# CONFLUENCE_SSL_VERIFY=false  # 자체 서명 인증서 사용 시

# JIRA_URL=https://jira.your-company.com
# JIRA_PERSONAL_TOKEN=your_personal_access_token
# JIRA_SSL_VERIFY=false  # 자체 서명 인증서 사용 시
